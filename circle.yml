dependencies:
  pre:
    - wget https://github.com/spf13/hugo/releases/download/v0.20.2/hugo_0.20.2-64bit.deb
    - sudo dpkg -i hugo*.deb

deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync AjinkyaBhosale/gitCircle-CI/public/* s3://amp-circle-ci/amp/ --delete
